# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian `osteoporosis/專案工作流程.md`。

## ⏯️ 目前做到哪

**2026-08-15**：骨折風險工具 `fracture-risk.html` 補上三個漏判的極高風險條件（**v1.1**）。
起因是肥胖醫學會演講要在投影片上標註本工具依循哪份指引，回頭做指引溯源時發現的。
完整溯源報告在**另一個 repo**：`C:\projects\obesity-society-ai-talk\reference\osteoporosis-guideline-scan-20260815.md`
（含 AACE 2020 R23、TOA 2025、Endocrine Society 2020、BHOF 2022 的逐項比對與出處）。

- 原本極高風險只實作五條，漏掉 AACE R23／TOA 2025 p.16 的三條，方向是**低估**——
  該優先給促骨生成劑的病人會被分到高風險組。已補：**長期類固醇使用期間發生骨折**、
  **近一年有傷害性跌倒**、**FRAX 主要骨鬆性 ≥30%／髖部 ≥4.5%**。
  （FRAX 那條原本只餵進高風險的 20%／3% 判定，填 32% 與填 22% 分層完全一樣。）
- 頁尾原本只寫「最新指引」，已改為指名版本並列出三處刻意保留的保守設定，見下方注意事項。
- 本機 14 個案例回歸測試通過。

**2026-08-09**：病人衛教單張 `patient-education.html` 改列印設定（**v1.1**）。
`@page` 由 `size:A4` 改為 A4∩Letter 交集 `210mm 279.4mm`，紙匣放哪一種都能直接印。
順帶修掉一個原本在 A4 下就已經存在的問題：第 5 面「居家檢查」那張卡單張 **287.4mm**，
比一頁可用高度還高卻掛著 `page-break-inside:avoid`，瀏覽器只能忽略、斷點落點失控。
改成卡片允許跨頁，改保護真正不該拆開的小單位（tile／note／藥物卡／Q&A／表格列／清單項），
標題以 `break-after:avoid` 黏住下一段。實測最高的不可拆單位由 287.4mm 降為 **105.6mm**。
只動這一頁，另外三頁沒碰。

在此之前：三個工具第一版完成並上線 GitHub Pages；專案初始化至第三層級。2026-07-28 這輪把「對外」與「位置」整理乾淨：
四頁加上署名（潘湘如醫師｜家醫科）＋版本號 v1.0、授權定為 CC BY-NC-ND 4.0 並附 LICENSE、刪掉 Drive 的過時 HTML 快照、
家用工作副本從 OneDrive 搬到 `C:\projects\osteoporosis-clinic\`，跨電腦改為純 git 同步。

## 🚦 目前狀態

- **可運行**。線上版四頁（入口 + 三工具）皆 HTTP 200，署名與授權區塊已在線上驗證。
- 工作副本乾淨、與 GitHub 同步。沒有做一半的東西。
- 骨折風險工具線上版已驗證為 v1.1（2026-08-15），含新增的三個極高風險條件與指引標註。
  **指引溯源報告列出的其他五項尚未處理**，見「下一步」第 0 點。
- 藥物選擇工具的「過去骨鬆用藥史」選項已簡化為純藥名（v1.1）；其餘工具內容自 07-27 起未再改動。
- 衛教單張列印設定已改（2026-08-09，v1.1），線上版已驗證含新的 `279.4mm` 設定。
  **只驗到「排版量測」這一層**（本機 http + 把 print 媒體查詢切成生效、版面鎖 180mm 實際量高度），
  **還沒真的送進影印機印一張出來**。

## ➡️ 下一步

0. **指引溯源報告裡尚未處理的項目**（2026-08-15 交班，依急迫性排序；
   逐條依據見 `obesity-society-ai-talk/reference/osteoporosis-guideline-scan-20260815.md` §五）：

   | # | 項目 | 現況 vs 指引 | 方向 |
   |---|------|-------------|------|
   | a | 另外三頁（`index.html`、`drug-selection.html`、`patient-education.html`）仍寫「最新指引」 | 未指名版本 | 標註 |
   | b | 極高風險第三條允許**單一**椎體骨折 | Endocrine Society 原文限**多發**椎體骨折 | 過寬 |
   | c | 高風險的「有脆弱性骨折史」未限部位 | AACE R20 限**髖部或脊椎**；BHOF 對肱骨/骨盆/遠端前臂且無骨質疏少者要求個別化 | 過寬 |
   | d | FRAX 20%／3% 未限定於 T-score −1.0 ~ −2.5 區間 | AACE R22 與 BHOF 都限定在**骨質疏少**族群 | 過寬 |
   | e | 缺 TOA 的「**中度風險**」層（MOF >10% 或髖 >1.5%） | 台灣指引對應「建議儘速接受骨密度檢查」，是門診有用的分流 | 缺層級 |

   b／c／d 三條方向都是**過寬**（會把人往上判），與 08-15 修掉的「漏判」相反，
   臨床急迫性較低，但要標指引就該一致。**動這三條會改變既有病人的分層輸出，改之前先確認。**

1. **門診試用新增的兩個極高風險條件**，特別留意「其他臨床危險因子」裡原有的
   「近一年曾跌倒」與新增的「近一年有傷害性跌倒（因跌倒受傷或就醫）」兩個框靠得近，
   語意雖有分（任何跌倒 vs 受傷就醫）但可能誤勾。
2. **實際印一張衛教單張出來看**（A4 紙匣、Letter 紙匣各一次），確認沒有被裁。
   列印視窗記得**取消勾選「頁首及頁尾」**、邊界選「預設」不要選「無」。
3. **門診實際試用三個工具**，收集要修的點（重點：藥物選擇的選藥順位是否符合實際習慣）
4. 決定要不要加固定的院內版健保給付欄——若要，需先提供目前依循的給付版本
5. 入口頁加一條連到肌少症工具的連結（要動另一個 repo，那邊的 handoff 也記著這條）
6. （選配）把可複用的部分做成醫療工具專案模板；（選配）產出給 OpenCode 學習的交接包

## ⚠️ 注意事項

- **repo 是公開的**（為了 GitHub Pages）。院內給付條文、病人資料一律不可進 repo。
- **流程就四步**：`git pull` → 改檔 → `commit` + `push`（Pages 約 1–2 分重建）→ 收工更新本檔再 push。
- **兩台電腦都有 Claude Code 與 GitHub，工作副本路徑一致＝`C:\projects\osteoporosis-clinic\`**（家用 DESKTOP-LVSV9Q5、醫院 X108521，2026-07-28 醫院端已 clone 並定位完成）。醫院無法安裝 Google Drive 桌面版（沒有 `G:\`，本專案已不依賴）。
- ⚠️ **不要用 OneDrive 同步這個 repo**。OneDrive 同步 `.git` 會造成 lock 衝突與衝突複本；跨電腦一律靠 git。
  檔案分工：程式碼走 git；Office 檔、分析產出、Obsidian vault 走 OneDrive；臨床原始資料集中 Google Drive。
- **改內容時頁尾版本號與日期要一起更新**（骨折風險工具 v1.1／2026-08-15、藥物選擇工具 v1.1／2026-07-28、衛教單張 v1.1／2026-08-09、入口頁 v1.0／2026-07-28）；署名與授權區塊不要拿掉。
- ⚠️ **極高風險的新條件都掛了 `hasOsteoporosis` 前置條件**（T ≤ −2.5 或有骨折史），不要拿掉。
  指引的極高風險七條是在「**已診斷骨質疏鬆症**」的病人裡再細分，不是獨立判準
  （AACE R23 最後一句就寫明未達極高者歸高風險）。沒有這道門檻，
  T-score −1.2、無骨折、只是跌倒擦傷的人也會被判成極高風險。
- **`fracture-risk.html` 有三處刻意與指引原文不同，都是較保守的設定，頁尾已明列**：
  近期骨折採 24 個月（指引 12 個月）、T ≤ −3.0（指引「低於 −3.0」）、FRAX ≥30%／≥4.5%（指引 >30%／>4.5%）。
  **要改回指引原文前先想清楚**：前者會讓更少病人被判極高風險。
- 改動分層邏輯後，用瀏覽器開本機版跑一輪案例回歸再推。**必測**
  `T −2.3、無骨折 → 低風險`——這是肥胖醫學會演講 P30 的示範個案截圖，輸出變了簡報就要重拍。
- **列印用的 `@page` 是 `210mm 279.4mm`（A4∩Letter 交集），不要改回 `size:A4` 或 `size:Letter`**。
  另外**任何 `page-break-inside:avoid` 的區塊都不能高於可用頁高**（開著瀏覽器頁首頁尾時只剩約 233mm），
  超過的話瀏覽器只會忽略它。改完版面要實際量，別憑看起來順眼。理由與量法寫在 `agents.md` 的專案特有守則。
- 用藥史選項只顯示藥名（bisphosphonate／denosumab／SERM 荷爾蒙／骨生成藥），但**判斷邏輯的語意仍是「denosumab＝目前仍在使用」「骨生成藥＝療程剛結束」**；輸出的提示文字保留完整敘述。
- **對外掛機構名稱前先確認權利歸屬**：目前刻意只署個人名（潘湘如醫師｜家醫科）、不掛醫院名。
- Google Doc（專案總覽）無法用 Drive 連接器修改，要改得用 claude-in-chrome 開瀏覽器；在 Docs 裡用座標點擊定位游標容易打偏，輸入前先截圖確認。
- 待清理：`C:\projects\_osteoporosis_舊OneDrive副本\`（舊工作副本，內容與 GitHub 相同，潘醫師確認後可刪）。

## 🕐 最後更新

- 時間：2026-08-15
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5（家）
- 版本：骨折風險工具 v1.1
- Git push：✅ 已推（`5811891` 工具改動；本次文件另計），線上版已驗證為 v1.1
- 前一筆：2026-08-09 @ DESKTOP-LVSV9Q5 · 衛教單張 v1.1 · ✅ 已推（`42db6ff`、`f873132`）
